/**
 * When using UtilityService.dynamicTableContainerMaxHeight,
 * an extra bit of buffer is needed to prevent the
 * scrollbar from still wanting to show up
 */
export const NGX_SCROLLBAR_BUFFER_PX = 57;
